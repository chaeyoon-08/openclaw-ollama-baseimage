# 구현 계획

## 구현 완료

```bash
git log --oneline
```

---

## 향후 계획

- feature/skills 브랜치: OpenClaw 빌트인 /skill + workspace 파일 (SOUL.md, AGENTS.md)로 에이전트 성격 부여
- feature/multi-agent 브랜치: AGENT_* 패턴 + A2A 오케스트레이션
- feature/agent-structure 브랜치: skills + multi-agent 합친 올인원 패키지
- API 키 노출 방지 (OpenClaw sandbox/tool 권한 설정 조사)
- preset_image/ 디렉터리 (프리셋 스킬 이미지)
